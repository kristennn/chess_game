<div class="col-md-12">
    <h5>所有用户 <%= link_to "新增推广员", signup_path, class:"btn btn-default-1" %></h5>

  <table class="table table-striped table-bordered">
    <thead>
      <tr>
        <th width="50%">用户名</th>
        <th>权限</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.permission %></td>
        </tr>
      <% end %>
   </tbpdy>
  </table>
  <div class="text-right">
    <%= paginate @users, window:2 %>
  </div>
</div>
