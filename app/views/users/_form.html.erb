<div class="container-row">
  <table class="table" border:"1px solid white">
    <tr>
      <td class="text-left" style="width: 50%"><strong>用户名称:</strong> <%= current_user.name %></td>
      <td class="text-left"><strong>推广ID:</strong><%= current_user.id %></td>
    </tr>
    <tr>
      <td class="text-left"><strong>代理等级:</strong> <%= current_user.permission%></td>
      <td class="text-left"><strong>账户余额:</strong> </td>
    </tr>

    <!-- <tr>
      <td class="text-left">下线玩家 : <%= current_user.tbl_accounts.count %> 位</td>
    </tr> -->
    <tr>
      <td class="text-left"><strong>提成总额:</strong><%= render_order(current_user) %></td>
      <% if current_user.permission == "县级代理" || current_user == "城市合伙人"%>
        <td class="text-left"><strong>分红金额:</strong></td>
      <% end %>
    </tr>
  </table>
</div>
